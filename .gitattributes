# 기본: 자동 정규화
* text=auto

# Windows 배치/스크립트는 반드시 CRLF (아니면 실행이 깨짐)
*.bat text eol=crlf
*.cmd text eol=crlf
*.vbs text eol=crlf
*.ps1 text eol=crlf

# 파이썬은 LF
*.py  text eol=lf

# CapCut 템플릿은 원본 바이트 보존 (변환 금지)
template/** -text
