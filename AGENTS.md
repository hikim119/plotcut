# PlotCut — 자막으로 리캡 영상 만들기

```
자막.srt  ──①──▶  <영화명>_타임라인 대본.txt  ──②──▶  CapCut 프로젝트
          대본 쓰기(너)                          컷편집(순수 Python)
```

**①이 이 문서의 핵심이다.** ②는 기계 작업이라 틀릴 일이 없다. 영상 품질은 전적으로 ①에서 갈린다.

이 문서 안의 경로는 전부 이 레포 기준 상대경로다. 사용자는 한국어로 소통한다.

---

## ① 대본 쓰기 — 절차

사용자가 자막(`.srt`/`.smi`/`.vtt`/`.ass`)을 주며 "대본", "리캡", "타임라인 대본"을 요청하면:

1. **이 문서를 끝까지 읽는다.** 포맷·구성·문체 규칙·분량 공식이 전부 여기 있다.
2. **`template/style_example.txt` 를 전문 읽는다.** 실제로 잘 나간 숏츠 3편의
   **나레이션 33문장 전문**이다. **여기 없는 문체는 쓰지 마라.**
   형식(제목·블록·문단 배치)은 이 파일이 아니라 아래 §포맷 을 본다.
3. **자막 전체를 읽는다.** 앞부분만 읽고 쓰면 반드시 실패한다 — 결말을 모르면
   감정선을 못 고르고, 어느 장면이 복선인지 판단할 수 없다.
4. 대본을 쓴다. 파일명 `<영화명>_타임라인 대본.txt`, 자막과 같은 폴더에.
   (도구가 만들 때는 뒤에 `_MMDD_HHMM` 시각이 붙는다.)
5. **줄거리를 파일로 남긴다.** 도구가 경로를 주면 대본을 쓰기 **전에**
   `[줄거리]` `[결말]` `[이 대본에서 고른 것]` `[자막에 안 나오는 것]` 네 항목으로
   정리해 쓴다. 사람이 편집 전에 읽는 용도다 — **자막에서 읽어낸 것만 쓰고**
   모르는 건 "자막만으로는 알 수 없음"이라고 적는다.
6. **검증을 돌려 `✘` 를 0으로 만든다** (아래 §검증).
7. 총 길이·블록 수·고른 줄거리를 3줄 이내로 보고한다.

목표 길이를 안 알려주면 **180초**로 잡고 그 값을 밝힌다.

---

## ① 포맷

```
좋아한다는 말 대신 몰래 그의 삶에 들어간 여자        ← 제목 (첫 줄, 괄호·대괄호 없음)

[01:25:03 ~ 01:25:07]                              ← 블록 헤더

우리 집에서 뭐 해요?                                ← 대사 문단 = 자막 큐 하나

시간 있으면 오랬잖아요                              ← 문단 구분은 빈 줄

잠시 보관해 줘요                                    ← 원본 큐가 2줄이면 2줄 그대로
나중에 와서 가져갈게요

(좋아하는 남자의 집에 몰래 들어왔다가 딱 걸려버린 여자)   ← 나레이션 = 괄호 안 한 줄
```

- **빈 줄이 문단 구분**이고, `[…]` 헤더가 나올 때까지가 한 블록이다.
- 헤더 시각은 **아무 값이나 대충 써도 된다** — 도구가 내용에서 다시 계산한다.
  다만 그 블록에서 인용한 대사가 전부 들어가는 범위여야 한다. **1.5초 이상 좁히면
  범위 밖 대사가 "직접 쓴 줄"로 강등되어 영화 음성과 자막이 어긋난다.**
- 헤더 뒤에 메모를 달 수 있다: `[01:06:10 ~ 01:10:20 / 집에 드나드는 구간]`
- **대사 없이 나레이션만 있는 블록**(b-roll 몽타주)도 된다. 그 경우 헤더 범위 안에서
  나레이션 수만큼 영상을 고르게 뽑아 쓴다.
- UTF-8(BOM 없음), 문단 사이 빈 줄 1개, 파일 끝 개행 1개.

### 자막이 한국어가 아닐 때 — `#자막번호` 로 쓴다

한국어 자막이면 **대사 원문이 곧 화면 자막**이라 그대로 옮기면 된다.
그런데 외국어 자막은 그대로 두면 영어가 화면에 나간다. 한국어로 갈아끼우면
이번엔 자막 원문과 글자가 하나도 안 겹쳐 컷 위치를 잃는다.

그래서 **텍스트 대신 자막 번호로** 큐를 가리킨다. 대본은 전부 한국어가 된다.

```
[00:09:35 ~ 00:10:10]

(전교에서 제일 잘나가던 여자였는데)

#176 대박, 저 귀걸이 좀 봐

#181 저기요, 이거 얼마예요?

#182-183 파는 거 아냐, 진짜 유물이거든
```

- `#176` 은 **SRT 파일에 적힌 번호**다. 사람이 자막을 열어 바로 찾을 수 있다.
- **한 문단 = 한 큐.** 큐가 바뀌면 문단도 바꾸고 번호를 새로 단다.
  연속 두 큐를 굳이 한 문단으로 합칠 때만 `#182-183` 처럼 **범위로** 적는다.
- **번호 없는 줄을 밑에 붙이지 마라.** 문단의 줄 수는 그 큐가 원래 가진
  줄 수와 같아야 한다. 자막이 2줄짜리(`Dad?` / `Maya?` 가 한 큐)면 2줄로
  쓰고, 1줄짜리 큐 둘이면 문단 둘로 쓴다:

  ```
  #763 아빠?          ✘ 763 은 한 줄인데 두 줄을 붙였다
  마야?                 → 마야? 가 763 구간에 같이 뜨고, 764 장면은 통째로 사라진다

  #763 아빠?          ✔ 큐마다 문단
                        (실측: 잘못 쓰면 컷 4.17초 → 2.24초로 줄어든다)
  #764 마야?
  ```

  `check` 가 `SRT#763 은 1줄인데 문단이 2줄입니다` 로 잡아 준다.
- **한국어가 아니면 예외 없이 전부 이렇게 써라.** 하나라도 빠지면 `check` 가
  `✘ 한국어로 안 바꾼 대사 문단이 N개입니다` 로 **실패**시킨다.
  자막 언어는 도구가 한글 비율로 자동 판별한다(실측: 한국어 99% / 영어 0%).
- 번역은 자막 직역이 아니라 **숏츠 화면에서 읽히게** 짧고 입말로 쓴다.
  실측(대사 248줄): 한 줄 **중앙 10자 · 최대 19자** · 한 문단 한 줄 ·
  쉼표 248줄에 1개 · **셋에 하나가 `?`·`!` 로 끝난다** · 영어 원문의 0.24배.
  `template/style_example.txt` §10 이 정본이다. **나레이션과 규범이 다르다.**
- **한국어 자막이면 번호를 쓰지 마라.** 원문을 그대로 옮기는 게 정석이고,
  그래야 사람이 대본을 고쳐도 텍스트로 다시 찾아낸다.

원문을 남겨두고 싶으면 `>` 로 번역을 덧붙이는 방식도 된다 — 매칭은 윗줄로,
화면에는 아랫줄로 나간다. 다만 대본이 영어투성이가 되니 기본은 `#번호` 다.

```
Why am I dressed like one of the Fenmore Foxes?
> 왜 하필 내가 라이벌 학교 옷을 입고 있는 건데?
```

---

## ① 어떻게 판단하는가

도구는 "무엇이 중요한지" 판단하지 않는다. **전부 너의 몫이다.**
순수 통계 휴리스틱(씬 경계·질문문·대화 밀집도)으로 시도했을 때 사람이 고른 49개 중
**6개만** 맞혔다(무작위가 2.7개). 인과와 감정을 읽어야만 나오는 판단이라서다.

### 1단계 — 감정선 하나를 고른다

영화 전체를 요약하려 들면 실패한다. **한 인물의 한 감정선**만 따라간다.
옴니버스처럼 이야기가 둘 이상이면 **하나만 쓰고 나머지는 통째로 버린다.**

**얼마나 고를 것인가** — 완성본 3편을 원본 자막과 짝지어 실측했다
(**대사 블록 32개** 전수 대조 — 소스 구간은 33개다. 한 블록이 7분 떨어진
두 구간을 이어 붙였다):

| 대본 | 영화 | 숏츠 | 쓴 구간 | 영화 대비 | 그 안의 대사 장면 | 시작~끝 |
|---|---|---|---|---|---|---|
| father | 22분 | 160초 | 18.5분 | 82% | 11개 | 9% ~ 92% *(22분 시트콤 1화)* |
| gentleman | 111분 | 111초 | 23.6분 | 1/5 | 12개 | 34% ~ 55% |
| robber | 97분 | 150초 | 12.3분 | 1/8 | 10개 | 13% ~ 25% |

> **긴 영화(40분 이상)는 이 3편처럼 한 대목만 쓴다.** 한때 「결말이 있는 전체
> 요약이 낫다」고 **판단만 하고** 전체 요약으로 갔는데, 8/27 에 재 보니 긴 영화
> 둘에서 **17:7(71%)** 로 한 대목 쪽이 이겼다. 도구는 영화의 88~90%를 훑고
> 있었다 — 12블록으로 그 폭을 덮으면 블록마다 7~8분씩 건너뛴다.
> **길이와 상관없이 모든 영화에 건다** (8/27, 유저분 지시).
> 한때 40분으로 잘랐다 — 22분 시트콤에서 1:11 로 졌기 때문인데, 그때 문구에는
> 「짧은 영화(30분 이하)면 전체를 써도 된다」는 **예외가 붙어 있었다.** 그래서
> 구간은 그대로인 채 대사만 빽빽해졌다(12~13블록/53~58대사 → 11~12블록/60~64대사).
> 진 건 「한 대목만」이 아니라 그 예외였던 셈이다. 예외를 빼면 짧은 영화도
> 실제로 한 대목을 잡는다. **짧은 영화는 아직 이 형태로 안 재 봤다.**

**한 대목을 골랐어도 장면 수를 늘리지 마라.** 블록은 10~12개다.
실측: 정답 3편이 블록 10~11개 · 블록당 대사 문단 5~10개 · 블록당 10~15초.
도구가 뽑은 전체 요약은 **18블록에 블록당 2.8문단 · 9.8초**였고, 그래서
장면마다 설명이 붙어 「요약 + 인용문」이 됐다.

그리고 **고른 구간 안에서는 대사를 촘촘히 쓴다. 다만 장면 길이에 따라 다르다** —
대사 블록 32개를 원본 자막과 짝지어 재면 이렇게 갈린다:

| 원본 장면 | 블록 수 | 숏츠 큐 ÷ 원본 큐 |
|---|---|---|
| **5큐 이하** (짧은 장면) | 13개 | **중앙 1.00** (한 줄을 둘로 쪼개 2.00 까지도) |
| **10큐 이상** (긴 장면) | 11개 | **중앙 0.61** (최소 0.12) |

즉 **짧은 장면은 거의 다 옮기고, 긴 장면은 넷 중 하나꼴로 버린다.** 짧은 장면에서
띄엄띄엄 고르면 화면이 비고, 긴 장면을 다 옮기면 늘어진다.

**외국어 자막이면 버리는 기준이 하나 더 있다 — 옮길 수 없는 줄은 통째로 버린다.**
말장난 · 그 나라 사람만 아는 밈 · 고유명사 나열 · 운율로 웃기는 말은 옮기면
길어지기만 하고 안 웃긴다. **그 큐는 번호째로 빼고 다음 큐로 간다.**
(실측 예: 원본 13큐짜리 장면이 완성본에서 6큐가 됐다 — 버린 7큐가 전부 이 종류다.)

> **「비율」과 「밀도」는 다른 것을 잰다.** 위 비율은 *고른 블록 안에서 얼마나
> 옮겼나*이고, `quality.py` 가 참고로 찍는 밀도(정답 18·30·60%)는 *사용 구간
> 전체에서 얼마나 골랐나*다. 층이 다르니 숫자가 달라도 모순이 아니다.

고르는 기준: 시작과 끝이 분명하고, 중간에 **상태가 바뀌는** 것.

### 2단계 — 그 감정선을 지탱하는 장면만 남긴다

고른 대사들이 **한 줄로 꿰여야** 한다. 잘 나온 대본의 48문단은 이렇게 이어졌다:

```
편지를 맡김 → 편지 안에 열쇠 → 몰래 들어감 → 집을 바꿈
→ 그는 모른 채 자기가 변함 → 데이트 신청 → 엇갈림 → 1년 뒤 재회
```

`편지 안에 열쇠가 들어 있었다`가 결정적인 이유는 **그게 없으면 뒤의 모든 행동이
성립하지 않기 때문**이다. 장면마다 물어라 —
**"이걸 빼면 뒤가 이해되지 않는가?"** 아니면 뺀다.

### 3단계 — 훅을 앞으로 뺀다

시간순으로 시작하면 지루하다. **중반 이후에서 가장 궁금해지는 대사 장면**을
첫 블록으로 올리고, 두 번째 블록부터 시간순으로 간다.

훅은 **답이 아니라 질문**이어야 한다. 결말을 훅으로 쓰지 마라.

### 4단계 — 나레이션으로 잇는다

나레이션은 **관객이 알아야 하는데 화면에 안 나오는 것**만 말한다.
방금 나온 대사를 요약·반복하면 안 된다.

```
(그런데 그 편지 안에는)                          ← 다음 대사를 열어준다
(하지만 그는 누가 자기 집에 들어왔는지도 모른 채)     ← 대사가 못 하는 상황 설명
(그리고 정확히 1년 뒤)                            ← 시간 점프
```

**완결형(`~다.`)으로 끊지 않는다.** 내 대본 33문장에 완결형이 **0개**다 —
`~는데` 36% · `~고` 30% · 명사 마무리 21% · `~죠` 9%. **열에 일곱이 문장을 안 닫는다.**

### 5단계 — 나레이션을 아낀다

**여기서 자연스러움이 갈린다.** 문장을 잘 써도 나레이션이 자주 끼면
이야기가 아니라 "요약 + 인용문"이 된다.

실제로 잘 나간 숏츠 **3편의 나레이션 33문장 전수 실측**이다
(`father` 시트콤 155초 · `gentleman` 액션 111초 · `robber` 스릴러 150초).
`중경삼림 참고본`은 문예 영화라 나레이션이 두 배 많다 — **왼쪽이 기준이다.**

| 항목 | **내 대본 (기준)** | 중경삼림 참고본 |
|---|---|---|
| **나레이션 간격** | **8~15초에 하나** (12.9/9.3/15.0) | 8.9초 |
| 한 덩어리 길이 | **중앙 16자** (7~31) | 중앙 21자 (6~44) |
| 21자 넘는 줄 | **열에 넷 미만** (17/25/20%) | 53% |
| 명사 마무리 | **다섯에 하나** (21%) | 32% |
| `~죠` | **편당 딱 1개** | 3개 |
| 완결형(`~다.`) | **0** | 0 |
| 마지막 문단 | **14자 이하 짧은 대사** (7/3/4자) | 19자 |

즉 **대사가 주인공이고 나레이션은 이음매**다. 한 블록에 대사가 6줄이면
나레이션은 0개나 1개지 3개가 아니다.

블록 배치는 이렇게 나온다 (`N`=나레이션, `D`=대사):

```
DDN  ND  DD  NDDDN  DN  NN  DD  NDDDDD  NDD
NNDD  NDDDDD  NNDDDD  NNDDNN  DDD  DDDD  DD  NDDDDDD  DD
```

- 대사가 스스로 설명하는 장면에는 **얹지 않는다**
- 나레이션은 **주로 블록 맨 앞**에서 다음 대사를 연다
- 연속은 **2줄까지**
- 문단당 인용 큐는 **94%가 1개**

`check` 가 `구성:` 줄과 `대사 N줄당 나레이션 1개` 를 찍어 준다. 위와 비교하라.

### 6단계 — 나레이션 문장을 다듬는다

내 대본 **33문장**에서 뽑은 네 가지 수법이다. 이게 "매끄러움"의 정체다.
전문은 `template/style_example.txt` 에 있다 — **그걸 먼저 읽어라.**

**① 말이 아니라 반응·상태 변화를 적는다.**
화면에는 보이는데 대사로는 안 나오는 것. 대사를 옮겨 적으면 같은 말을 두 번 듣는다.

```
일진은 좀 쫄았고            처참히 진압됐고
닉은 미동도 없었고          그대로 멘탈이 나가버렸고
일진들은 좀 구미가 당겼는데   닉의 진짜광기에 티폴은 질려버렸죠
```

**② `[상황]한 [인물]` 명사 마무리** — 33개 중 7개(21%). 다음 장면의 주인공을 지목하며 넘긴다.
**다섯에 하나만 써라.** 이게 제일 과하게 쓰이는 수법이다 — 도구가 뽑은 대본은
이 형태가 60~70%까지 튀어 문장이 전부 툭툭 끊겼다.

```
가게에서 애들이 시끄럽게 굴자 짜증이 난 미키
그렇게 고속도로에 진입한 둘
아까 지갑을 던진게 떠오른 닉
일이 단단히 잘못됐단 걸 깨달은 닉
결국 인내심에 한계가 온 미키
```

**③ 접속부사로 연다** — `그 말에` `그렇게` `결국` `일단` `하지만 계속` `아까` `곧바로` `그때`.
앞 장면과 이어 붙이는 접착제다. 없으면 문장이 툭툭 끊긴다.

```
그 말에 일진들은 냅다 도망치기 시작했는데
일단 휴전한 둘은 식당에 들어갔는데
하지만 계속 잼민이들과 엮이는데
```

**④ 영화의 말투를 그대로 쓴다.**
액션·코미디면 구어체와 은어(`잼민이` `틀딱` `쫄았고` `참교육` `냅다` `맛탱이가 가버렸고`),
문예물이면 문어체. **영화 톤과 나레이션 톤이 어긋나면 그게 제일 어색하다.**

종결 분포(33문장): `~는데` 36% · `~고` 30% · 명사 21% · `~죠` 9% · 완결형 **0**.
**절반 가까이가 `~는데`** — 다음 대사가 답을 주기 때문이다.

---

## ① 문체 규칙

1. **줄거리 하나만.** 옴니버스·다중 화자면 감정선이 가장 뚜렷한 하나를 고르고
   나머지는 통째로 버린다.

2. **첫 블록은 훅.** 중반 이후에서 가장 궁금해지는 대사 장면으로 시작하고,
   두 번째 블록부터 시간순으로 간다. (샘플: 01:25 → 00:51 → 00:56 → … → 01:39)

3. **나레이션은 대사가 말하지 않는 것만 말한다.** 방금 나온 대사를 요약·반복하지 않는다.
   대사가 감정을 하고, 나레이션은 그 사이를 잇는다.

4. **완결형으로 끊지 않는다.** `~다.` 로 끝내지 말 것.
   샘플 19줄 실측: 연결어미 13줄(`~는데` 2 · `~고` 5 · `~지만` 2 · `~채` 1 · `~죠` 3),
   명사·부사 마무리 6줄(`…엇갈린 두 사람`, `…정확히 1년 뒤`). **완결형 0줄.**

5. **나레이션 한 줄 = 한 문장, 중앙 16자.** 33문장 실측 7~31자.
   **21자를 넘는 줄은 열에 넷 미만**이다 — 넘으면 낭독이 늘어져 대사 리듬이 죽고
   자막이 화면 3줄이 된다.
   **짧은 건 문제가 아니다** — `father` 는 15자 미만이 67%다. 길어지는 것만 막으면 된다.
   - 반응 한 마디: `일진은 좀 쫄았고` `처참히 진압됐고` `닉은 미동도 없었고`
   - 뒤 대사를 열어주는 도입구: `그런데 그 편지 안에는`
   - 시간·상황 전환구: `그리고 결국` `그리고 정확히 1년 뒤`
   **나레이션** 자막 한 줄은 **14자를 절대 안 넘는다**(나레이션 큐 58개 전수).
   **대사 줄은 최대 19자다**(대사 248줄 전수) — 두 규범이 다르다. 긴 문장은 2~4줄로
   쪼개지는데, 33문장 중 1줄 12개·2줄 18개·3줄 2개·4줄 1개다 — **기본은 1~2줄.**

6. **대사는 자막 원문 그대로 — 자막이 한국어일 때.** 줄바꿈도 원본대로 옮긴다.
   외국어 자막이면 대사를 **직접 쓴다** — §자막이 한국어가 아닐 때 를 따른다.
   거기선 이 규칙이 아니라 **대사 248줄 실측**이 기준이다(한 줄 중앙 10자·최대 19자).
   - 한 문단에 **연속 큐 최대 2개**까지 합칠 수 있다.
   - **한 종결어미로 몰지 마라.** 실측 `~는데`:`~고`:명사 ≒ 4:3:2 이고,
     한 종류가 **절반을 넘은 편이 없다**(최대 45%). `~고` 만 반복하면 낭독이
     단조로워진다 — 도구가 실제로 58%까지 몰린 적이 있다.
   - 읽기 편하게 쉼표에서 줄을 다시 나누거나 두 줄을 한 줄로 펴는 정도는 괜찮다
     (`check`가 공백·문장부호를 무시하고 대조한다). 단어를 바꾸면 매칭이 깨진다.
   - **자막에 없는 대사를 지어내지 않는다.**

7. **결말까지 간다.** "과연 어떻게 됐을까요?" 식 낚시 금지.
   마지막 블록은 결말 대사로 끝낸다.
   **마지막 문단은 14자 이하 짧은 대사 한 방이다** — 실측 `알아`(2자) / `알겠어`(3자) /
   `가볼까?`(4자). **나레이션으로 정리하지 마라.** 3편 전부 대사로 끝난다.

8. **인물 호칭은 자막에 나온 것을 우선한다.**
   - 표기가 갈리면(`633`/`663`) 인물 본인이나 상대가 정정하는 쪽을 정본으로 삼는다.
   - 자막에 이름이 없으면 지어내지 말고 역할 호칭(`그 여자`, `경찰`, `점원`)을 쓴다.
   - 널리 알려진 통용 이름은 써도 된다(샘플의 `페이`는 자막에 0회 등장한다).
     `check`가 "자막에 없는 이름"으로 표시하니 의도한 것인지 확인만 하면 된다.

9. **분량** — 절대값보다 **비율**이 먼저다.

   **나레이션은 8~15초에 하나.** 이건 영화가 뭐든 변하지 않는다.
   (대사 "줄" 은 자막 큐냐 화면 줄이냐에 따라 두 배씩 흔들린다 — 초로 세라.
    180초면 나레이션 12~20문장이다.)

   줄 수는 영화가 정한다. 대사가 짧고 빠른 액션·코미디는 초당 대사가 두 배다:

   | | 초당 대사 | 초당 나레이션 |
   |---|---|---|
   | 액션·코미디 (`gentleman`/`robber`) | 0.52줄 | 0.07~0.10덩어리 |
   | 문예·드라마 (중경삼림) | 0.285줄 | 0.113덩어리 |

   → 180초 액션이면 **대사 90줄 · 나레이션 15덩어리**,
     180초 드라마면 **대사 50줄 · 나레이션 18덩어리**.

   첫 초안용 눈금일 뿐이다. **정확한 길이는 `check`가 그 영화 자막에서 계산해 알려준다.**

---

## ① 절대 하면 안 되는 것

| 금지 | 왜 |
|---|---|
| **자막에 없는 대사를 지어내기** | 화면 자막과 실제 음성이 어긋난다. 그 컷은 못 쓴다 |
| 대사를 매끄럽게 다듬기 | 같은 이유. 쉼표에서 줄을 다시 나누는 정도만 허용 |
| 자막 앞부분만 읽고 쓰기 | 결말을 모르면 감정선을 못 고른다 |
| 결말을 빼고 낚시로 끝내기 | 마지막 블록은 결말 대사로 끝낸다 |
| **장면 수를 늘려 영화를 훑기** | 100분이어도 블록 10~12개. 잘게 썰면 「요약 + 인용문」이 된다 |
| **고른 장면 안에서 대사를 띄엄띄엄 고르기** | 짧은 장면(5큐↓)은 거의 다 옮긴다(비율 1.00). 긴 장면(10큐↑)은 0.61 — **장면 길이로 갈린다** |
| 블록 범위를 대사에 딱 맞춰 좁히기 | 앞뒤 나레이션 자리가 없어진다. **범위는 대충 써도 된다 — 도구가 다시 계산한다** |
| 한 문단에 연속 큐 3개 이상 | 상한 2개 |
| **나레이션으로 대본을 끝내기** | 3편 전부 짧은 대사로 끝난다. 나레이션은 이음매지 마침표가 아니다 |
| **나레이션을 21자 넘게 쓰기** | 33문장 중 열에 넷 미만이다. 넘으면 자막이 3줄이 되고 낭독이 늘어진다 |

**자막 번호는 SRT 파일에 적힌 번호로 본다.** 도구 내부 인덱스(`idx`)와
파일 번호(`src_no`)는 노이즈 큐가 빠지면서 어긋난다.

---

## ① 검증 — 통과시킨 뒤 보고한다

```
python cli.py check "<대본>.txt" --srt "<자막>.srt" [--movie "<영화>.mp4"] --seconds 180
```
영화는 선택이다(없으면 길이·싱크 검사만 빠진다).

| 표시 | 뜻 | 대응 |
|---|---|---|
| `✘ 자막에서 찾지 못한 대사 문단` | 지어냈거나 오타 | **자막 원문으로 고친다. 0이 될 때까지** |
| `✘ 문단 하나가 N초를 덮습니다` | 너무 먼 큐 둘을 한 문단에 | 문단을 나눈다 |
| `· 같은 대사를 두 번 인용한 문단` | 훅으로 앞에 뺀 대사를 제자리에서 또 썼다 | 한쪽을 지운다 (참고 — 승패와의 관계는 확인 안 됨) |
| `· '이름' 는 대본 마지막 25%에서 처음 등장` | 결말에서 소개 없이 나오는 인물 | 그 전 나레이션에서 역할·이름으로 소개한다 (참고 — 게이트 아님) |
| `⚠ 목표에서 ±N초` | 분량 | 문단/나레이션 줄 수 조정 |
| `⚠ 블록N이 블록M보다 앞으로` | 시간 역행 | 훅이면 정상, 아니면 순서 조정 |
| `⚠ 결말이 빠졌을 수 있습니다` | 마지막이 영화 70% 이전 | 결말 블록 추가 |
| `✘ 한국어로 안 옮긴 대사 줄` | 번호는 달았는데 원문이 남았다 | 그 줄을 한국어로 다시 쓴다 |
| `⚠ 대사 줄 중앙이 N자` | 직역 | §자막이 한국어가 아닐 때 — 내 대본 중앙 10자 |
| `⚠ ?·! 로 끝난 대사가 N%` | 대사를 나레이션처럼 평서화 | 내 대본은 셋에 하나가 `?`·`!` |
| `⚠ 쉼표 든 대사 줄이 N%` | 원문 쉼표를 그대로 옮김 | 쉼표 자리면 줄을 끊는다 (내 대본 248줄에 1개) |
| `✘ 나레이션이 N초마다 하나입니다` | 나레이션 **간격**(초) | 나레이션 수를 늘리거나 분량을 줄인다 (기준 6~16초 · 내 대본 9.3/14.1/15.0) |
| `✘ 대본이 나레이션으로 끝납니다` | 나레이션이 마침표 노릇 | 마지막은 대사다 — 나레이션은 이음매다 |
| `⚠ SRT#a-b 는 연속 큐 N개입니다` | 번호 범위를 넓게 잡았다 | 상한 2개. 넘기면 자막 N줄이 한 줄로 뭉개진다 |
| `⚠ 나레이션 평균 길이` | 문체 | 규칙 5 — **중앙 16자**, 21자 넘는 줄은 열에 넷 미만 |
| `⚠ 나레이션이 ~고 로만 N% 끝납니다` | 한 종결어미로 몰림 | 몇 개를 `~는데` 로. 실측 `~는데`:`~고`:명사 = 4:3:2 |
| `⚠ 대사 N줄마다 나레이션이 하나` | **나레이션이 대사를 밀어낸다** | 5단계 — 내 대본은 대사 5~8줄에 하나 (단위는 **줄**이다. 초 단위 기준은 바로 위 간격 행) |
| `⚠ 나레이션 없는 블록이 N%뿐입니다` | **나레이션이 너무 자주 끼어든다** | §5단계 — 대사만으로 통하는 블록에서 뺀다 |
| `⚠ 나레이션이 N줄 연속됩니다` | 설명만 흐르는 구간 | §5단계 — 2줄까지 |
| `⚠ 완결형으로 끊긴 나레이션` | `~다.` `~요.` 로 문장이 닫힘 | 규칙 4 — 연결어미나 명사구로 넘긴다 |
| `· 동일 후보 N건 중 SRT#x 선택` | 같은 대사가 그 범위에 여럿 | 의도한 쪽인지 확인 |
| `· 자막에 없는 이름` | 통용 이름 사용 | 의도했으면 그대로 |

**참고 수치** (잘 나온 대본, 168.5초): 블록 18 · 대사 문단 48(인용 큐 51) ·
나레이션 19줄 409자 · 매칭 서명 45 / 2큐병합 3 / 강등 0.

---

## ① 흔한 실수

- **블록마다 나레이션을 단다.** 가장 흔하고 가장 티 나는 실수다. 대사가 스스로
  말하는 장면에는 얹지 않는다 — 잘 나온 대본은 블록 셋 중 하나가 대사뿐이다(§5단계).
- **나레이션이 방금 나온 대사를 다시 설명한다.** `(좋아하는 남자의 집에서 나오다
  그와 딱 마주친 여자)` 처럼 바로 위 대사를 풀어 쓰면 관객은 같은 말을 두 번 듣는다.
- **자막 번호를 재번호로 착각한다.** 도구 내부 인덱스와 SRT 파일 번호는 다를 수 있다
  (노이즈 큐가 빠지면서 어긋난다). 사람이 보는 자리는 전부 **SRT 파일 번호**다.
- **나레이션을 두 문장으로 쓴다.** 한 줄 = 한 문장.
- **대사를 매끄럽게 다듬는다.** 다듬으면 화면 자막과 실제 음성이 달라진다.
- **블록 범위를 대사에 딱 맞춰 좁힌다.** 앞뒤 나레이션이 들어갈 자리가 없어진다 —
  범위는 넉넉히 두거나 아예 대충 써도 된다(도구가 다시 계산한다).
- **훅을 결말로 쓴다.** 훅은 궁금하게 만드는 장면이지 답이 아니다.

---

## ② 편집 — 도구 쓰는 법

```
python cli.py build [<대본.txt>] --srt <자막.srt> [--movie <영화.mp4>] [--name <이름>]
python cli.py script <자막.srt> [--seconds 180] [--extra "..."]
python selftest.py   # 회귀 전부 — 코드를 고쳤으면 반드시
```

- **대본을 생략하면** `script_gen.py` 가 로그인된 에이전트 CLI(`codex`/`claude`)를
  하위 프로세스로 불러 먼저 만든다. API 키 없음 — 구독 로그인을 그대로 쓴다.
- **영화를 생략하면** 자막 트랙 + 타임라인 길이만 든 프로젝트가 된다.
  나중에 같은 이름으로 영화를 주고 다시 돌리면 그 자리에서 영상이 채워진다.
- **자막 + 영화를 함께 줬으면 대본만 쓰고 멈추지 마라.** 이어서 `build` 까지 하고
  한 번에 보고한다.
- CapCut을 꺼야 할 필요는 없다. 늘 **새 폴더**만 만들고 기존 프로젝트를 건드리지
  않는다. 다만 CapCut은 켤 때 드래프트 폴더를 훑으므로 **다시 켜야** 목록에 뜬다.
- **프로젝트명을 안 주면 `<영화명>_MMDD_HHMM`** 이 된다. GUI 는 넣은 파일 이름으로 채워 준다.
- **`--freeze`(기본 켜짐)** 이면 결과 대본에 컷 시각(`@…~…`)이 박혀, 다음부터
  **대본 + 영화만으로** 만들 수 있다. 대신 대사를 고쳐도 컷은 안 움직인다.
- 자막도 시각도 없으면 블록 범위에 균등 배치된다(대사 순간과 어긋남).
- **덮어쓰기를 하지 않는다.** 같은 이름이면 `이름_2`, `이름_3` 으로 새로 만든다 —
  결과 폴더와 CapCut 프로젝트 폴더가 같은 이름으로 짝을 맞춰 붙는다.
  그래서 편집 감지·백업·되돌리기가 없고, CapCut 을 켜 둔 채로도 만들 수 있다.
- **나레이션 더빙은 도구가 넣지 않는다.** `[나레이션 복사]` 로 문구를 받아 TTS를
  만든 뒤 CapCut에 직접 끌어다 놓는다. 도구가 넣으면 재생성 때마다 CapCut에서
  한 편집이 날아가는데, 나레이션 길이 추정이 실측 대비 중앙 10%%밖에 안 틀려
  (실측 114 ms/단위 vs 기본값 120) 자동화할 값어치가 없었다.

---

## 코드를 건드릴 때

- **`python selftest.py` 를 반드시 돌린다** — 「전부 정상」이 나와야 한다.
  **건수는 문서에 안 적는다.** 검사를 넣을 때마다 어긋나고, 실제로 하루에
  세 번 틀렸다(79 → 209 → 423 → 451). 틀린 숫자는 「이 문서는 낡았다」는
  신호만 준다.
  단 회귀 테스트는 `test/` 의 자막·영화가 있어야 돈다. 저작물이라 레포에 넣지 않았다.
- **사용자의 실제 CapCut 폴더를 테스트로 건드리지 않는다.** 실험은 `--draft-root <임시폴더>`.
- **테스트용 영화 파일은 수 GB다. 절대 열지 말 것**(ffprobe만).
- LLM API를 쓰던 이전 설계(`align.py`·`plan_llm.py` 등)는 **지웠다**. 되살리지 않는다.
  이 도구 안에는 LLM이 없다 — 대본은 외부 에이전트 CLI가 쓴다.
- **`.bat`/`.vbs`/`.ps1` 은 CRLF + 내부 ASCII** (cmd 코드페이지 949).
  PowerShell 5.1 이라 `&&`/`||`/삼항 연산자가 없다.

### 대본 품질은 **쌍대 대결**로만 잰다

자동 지표로는 못 가른다. 같은 프롬프트로 두 번 돌리면 블록 ±1 · 대사 ±4 ·
명사끝 **±15%p** 가 흔들리고, gentleman 은 쓴 구간이 **89.9분 vs 23.7분**으로
갈렸다. 변형 효과보다 판마다의 편차가 크다. `cli check` 도 요즘 대본은
경고 0건이라 더 안 갈린다.

그래서 `bench.py` 로 **같은 영화 · 블라인드 · 순서 반전** 쌍대 비교를 한다.
심사자는 우열을 못 가르면 **전원 1번을 찍으므로**(자기 검사 9표 전원) 순서를
반드시 뒤집고, **짝이 안 맞는 관점은 버린다** — 죽은 표 하나가 50/50 을
55.6/44.4 로 민다.

**눈금**: 도구 대본 vs 유저분 완성 숏츠 3편, 30표 → **도구 57%**.
관점별로는 `ending` 6/6 · `gut` 4/6 · `hook` 3/6 · `fun` 3/6 · **`flow` 1/6**.
대본은 이미 나쁘지 않고 **따라가지는가 하나만** 못한다.

심사평 54건에서 제일 많이 나온 말: **훅 46%** · **맥락 없이 튀어나온다 30%** ·
누가 말하는지 20%. 특히 **처음 나오는 인물을 소개 없이 쓴다**가 반복해서
걸렸다(`로이스` `테런스` `조 아빠`).

| 프롬프트 변형 | 승률 | |
|---|---|---|
| `v2_dense` 대사를 잘게 | 75% (6/8) | 개별로는 이김 |
| `v4_context` 화자·셋업·줄기 | 67% (8/12) | 개별로는 이김 (robber 4/4) |
| `v3_nostyle` 문체예시 지시 제거 | 50% (4/8) | 차이 없음 |
| `v1_reaction` 더 반응체로 | 38% (3/8) | 졌다 |

**그런데 이긴 둘을 합쳐 넣고 검증 세트로 다시 붙였더니 44%(7/16)로 졌다.**
father 3/4 · robber 2/4 · **gentleman 0/4** · 중경삼림 2/4.
gentleman 패배 사유가 프롬프트가 시킨 것과 정반대였다 — 「인물 이름 소개가
없고 초반 8줄이 설명 없이 터지고 같은 대사가 반복된다」. 지시가 그 판에서
**안 먹은 것**이지 지시가 나쁜 게 아니다.

**그래서 기본 프롬프트는 안 바꿨다**(`c0274042ddce636c`). 개별 승(6/8·8/12)도
합친 패(7/16)도 동전 던지기로 흔히 나오는 크기다. 생성 편차가 워낙 커서
**조건당 한 판으로는 못 가른다** — 다시 하려면 조건당 3판 이상 뽑아 놓고
대결해야 한다. 변형 넷은 `bench.py gen --variant v2_dense` 로 남아 있다.

### 2차 (8/26) — 편차의 실체, 기계 검사 둘, 후보 옵션

22개 벤치 대본을 다시 재니 「편차」는 **gentleman 한 편**의 문제였다 — father·robber 는
7판이 전부 같은 구간·같은 훅, gentleman 만 7판 중 5판이 다른 줄기(22% 구간). 그리고
flow 에서 진 판이 왜 졌는지가 **기계로 잡히는 둘**로 갈린다:

| 검사 | 근거 | 급 |
|---|---|---|
| **훅 대사 재인용** — 같은 큐를 두 번 인용한 문단 | 레퍼런스 3편 0 · 도구 판에는 나온다 | ⚠ 참고 (한때 `✘`, 아래를 보라) |
| **늦은 이름** — 마지막 25%에서 처음 나오는 이름 | 심사평이 지목한 로이스·테런스·코치가 정확히 잡힌다 · 레퍼런스 0 | 참고 — 오탐 표본 없음 |

**정정 — 재인용을 `✘` 로 올린 근거는 틀렸다.** 「진 판이 전부 이걸 갖고 있었다」고
적었는데, 진 대본만 들춰 보고 **이긴 대본을 안 봤다**(밑수 오류). 심사표 82개를
A/B 와 대본 파일까지 짝지어 다시 재니:

| | |
|---|---|
| 재인용 수가 갈리는 표 | 12개 = **독립 대본쌍 3개**(한 쌍당 4표) |
| father_v2_dense(2) vs base(0) | **재인용 있는 쪽이 3/4 승** |
| father_v1_reaction(2) vs base(0) | flow 2승 / gut 2패 |
| gentleman_ship(2) vs base(0) | 0인 쪽 4승 |

셋이 서로 엇갈린다 — 승패를 예측하지 못한다. 나레·대사 밀도 지표(나레 뒤 대사
비율 47% · 최대 연속 대사 55% · 나레 비율 55%)도 전부 동전 던지기였다. 심사평의
「셋업 없는 펀치」는 **그 대사에** 셋업이 없다는 말이지 나레가 적다는 말이 아니다.
총량 지표로는 안 잡힌다.

그래서 재인용은 **⚠ 참고**로 낮췄다(3분짜리에서 같은 줄을 두 번 쓰는 건 낭비고
레퍼런스는 0이지만, 증거 없이 강제로 고치게 하지는 않는다). selftest [32] 가
심각도를 못 박는다. `✘` 로 올리려면 **재인용만 다른** 대본쌍으로 승률을 재라.

프롬프트 문구 둘도 고쳤다(해시 `ce797c97`): 「2초 안에 주인공」→ 콜드오픈 + 훅 대사
재인용 금지 · 「자막에 없는 이름 금지」→ 규칙 8(통용 이름)과 맞추고 첫 발화 전 소개.

**순위 심사는 재현되지 않는다 — 관문 미달(사전 등록).** 「같은 대본 3개를 셔플만
바꿔 두 번 재워 τ ≥ 0.5」로 미리 정했고, 기존 대본 19개로 21회 재웠다(생성 0회):

| | |
|---|---|
| 평균 켄달 τ | **0.333** (9쌍 중 4쌍만 일치, 2쌍은 −1 로 완전 역전) |
| 1위 재현 | 5/9 (56%) — 우연 33% |
| 순환 3순서를 평균낸 합의 1위 | **3편 중 1편**만 재현 |
| 같은 대본 3개를 넣었을 때 | 3/3 이 「1번」 — 못 가르면 1번을 찍는 편향은 실재 |

순환 순서는 그 편향을 상쇄할 뿐, 판정을 재현시키지는 못한다. **조건 비교는
쌍대 대결로 돌아간다**(§대본 품질은 쌍대 대결로만 잰다).

**서로 다른 대본 N개 뽑기(옵션).** `--candidates 3` / GUI 체크박스. 후보를 순차로
뽑아 전부 남기고 「어느 구간·대사 수·줄거리 첫 줄」을 한 줄씩 보여 준다. 기본은
후보1로 이어 만든다. **에이전트가 고르지 않는다** — 위 관문 미달 때문이다. 파는
것은 「가장 좋은 대본」이 아니라 **선택지**다(gentleman 은 7판 중 5판이 다른 줄기).
후보가 서로 같으면 그렇게 말한다. `rank_candidates` 는 실험용으로만 남는다.

2차 실험(조건 base · 이름 · 자기점검 × 3편 × 3판, 3개 블록 순환 순위)은 진행 중이다.
결과는 이 절에 이어 쓴다.

### 3차 (8/26 밤) — 두 에이전트 × 두 조건 × 108표

Codex 사용량 한도로 실험이 끊겨 **클로드로 한 벌을 통째로 다시 뽑았다**(기준선
포함 27판). 그 덕에 원래 계획보다 나은 게 생겼다 — 같은 조건을 **두 에이전트에서
독립으로** 잰 것이다. 심사는 쌍대(순서 뒤집기 필수), 판정자는 sonnet.

| 조건 | 에이전트 | 전체 | 종합 | flow | father | robber | gentleman |
|---|---|---|---|---|---|---|---|
| **이름** | 클로드 | 21:15 (58%) | 12:6 | 9:9 | **10:2** | 4:8 | 7:5 |
| **이름** | Codex | 21:15 (58%) | 8:10 | 11:7 | 7:5 | 5:7 | **9:3** |
| 자기 점검 | 클로드 | 19:17 (53%) | 10:8 | 9:9 | 6:6 | 4:8 | 9:3 |

**이름 조건은 두 에이전트에서 정확히 같은 58% 가 나왔다**(각 21:15, 합쳐 42:30).
순서를 뒤집어도 답이 같은 쌍만 세면 16:10(62%). 그래도 **미리 정한 관문 2/3 에는
미달**이고 이항검정 p≈0.08 이다 — 그래서 아직 안 넣는다.

**자기 점검은 수행됐는데 효과가 없었다.** 흔적 3종이 9/9 남았고(줄거리
`[자기 점검]` 절 · 보고 줄 · 로그) 8/9 가 실제로 고쳤다고 보고했다
(`점검: ⓒ✗→고침 ⓓ○`). 그런데 53%. 「스스로 점검해라」는 지시는 대본을 바꾸긴
하지만 더 낫게 바꾸지는 않는다.

**기계 지표는 이번에도 못 갈랐다.** 108표에 대고 세 가지를 다시 쟀다 —
나레 없이 이어진 대사 최대(53%) · 나레 뒤에 오는 대사 비율(55%) · 나레에서
이름으로 불리는 인물 수(63%, 쌍 단위로는 10/15 = 67% · p≈0.15). 셋 다 관문
근처도 못 간다. 이름 수는 그나마 높지만 **조건 자체와 겹치는 값**이라(이름
조건 대본이 당연히 이름이 많다) 독립 증거가 아니다. 「나레가 매 전환마다 주체를
짚어 준다」는 심사평이 아무리 반복돼도, 그걸 총량으로 세는 지표는 승패를 못 맞힌다.

**robber 는 두 에이전트에서 모두 이름 조건이 졌다**(4:8 · 5:7). 심사평이 원인을
말한다 — 「내레의 '티폴'과 대사의 '테런스'가 어긋나고」, 「같은 인물이 두 이름으로
나와 줄기가 흔들린다」. 이름을 쓰라고 하면 **한 인물을 두 이름으로 부르는** 새 실패가
생긴다. 그래서 「한 인물은 대본 전체에서 한 이름으로만」을 넣은 판(nameF)을 다시 쟀다.
Codex 가 또 한도에 걸려 클로드로 돌렸고, 기준선도 같은 클로드(base4)라 비교는 성립한다.

### 4차 (8/27 새벽) — **관문 통과. 이름 조항을 넣는다**

| | 전체 | 종합 | flow | father | robber | gentleman | 순서 일치 |
|---|---|---|---|---|---|---|---|
| nameC (클로드) | 21:15 58% | 12:6 | 9:9 | 10:2 | **4:8** | 7:5 | 15/18 |
| nameN (Codex) | 21:15 58% | 8:10 | 11:7 | 7:5 | **5:7** | 9:3 | 11/18 |
| **nameF (개선판)** | **24:12 67%** | 12:6 | 12:6 | 8:4 | **8:4** | 8:4 | **18/18** |

한 줄을 넣었을 뿐인데 세 가지가 동시에 좋아졌다:

- **robber 가 4:8 → 8:4 로 뒤집혔다.** 두 에이전트에서 지던 유일한 영화였고,
  심사평이 지목한 원인(내레의 '티폴' vs 대사의 '테런스')이 사라졌다.
  실제로 nameC 는 robber 3판 중 2판이 두 이름을 섞어 썼고 nameF 는 0판이다.
- **flow 가 9:9 → 12:6.** 이름이 흔들리면 「누가 뭘 하는지」가 흔들린다.
- **순서를 뒤집어도 18/18 쌍이 같은 답.** 앞선 두 판은 15/18 · 11/18 이었다.
  심사자가 망설이지 않는다는 뜻이다.

세 영화가 전부 8:4 로 고르다 — 한 영화가 끌어올린 평균이 아니다.
미리 정한 관문(조건당 2/3)을 만족하므로 **프롬프트에 넣는다**:

```
**이 작품을 안다면 그 지식을 이름에 써라.** 자막에 이름이 안 나오는
인물도 널리 알려진 이름이 있으면 그 이름으로 불러라 — 역할 호칭만 남기지 마라.
**한 인물은 대본 전체에서 한 이름으로만 불러라.** 나레이션과 대사에서
다른 이름(성·애칭·약칭)이 섞이면 보는 사람이 다른 사람으로 안다.
```

**이건 대결로 이겨서 기본 프롬프트에 들어온 첫 문구다.** 앞선 변형 넷(v1~v4)은
전부 검증에서 떨어졌다.

### 자리가 효과를 만든다 — 12:0

처음엔 이 문구를 **문체 블록 안**(이름 관련 줄들 옆)에 심었다. 자연스러운
자리처럼 보였다. 그 프롬프트로 3판을 새로 뽑아 홀드아웃을 돌리니 7:5(58%)로
힘이 빠졌고, 실험이 이긴 판(`--extra`, 맨 끝)과 **직접 붙여 보니**:

| | |
|---|---|
| `--extra`(맨 끝) vs 문체 블록 안 | **12:0** |
| 순서를 뒤집어도 같은 답 | **6/6** |

같은 문구인데 전패다. 내용이 아니라 **자리**다 — 긴 문체 블록의 한 줄로 묻히면
안 지켜진다. 그래서 이름 블록은 프롬프트 **맨 끝**, 사용자의 `추가 지시(최우선)`
바로 앞에 둔다. selftest [23] 이 그 자리를 검사한다(끝에서 10줄 안).

**교훈: 프롬프트에 반영할 때는 실험이 이긴 자리에 그대로 놓아라.** 문구만
옮겨 심으면 실험 결과가 안 따라온다. 반영 뒤에는 **원판과 직접 붙여** 같은지
확인해라 — 기준선과만 비교하면 힘이 빠진 걸 못 잡는다(7:5 는 그냥 표본이
작아 보였다).

해시 핀 `ce797c97` → `30a6d30e`. 이름 블록이 문체 블록 밖이라 `_canon_prompt`
가 **그 블록도 같이 찍도록** 고쳤다 — 안 그러면 통째로 지워도 해시가 안 변한다.

### 홀드아웃 — 통과 (반영판이 원판보다 낫다)

맨 끝 자리로 고친 프롬프트로 9판을 **새로 뽑아** 두 방향으로 쟀다(72표):

| 붙인 상대 | 결과 | 종합 | flow | father | robber | gentleman |
|---|---|---|---|---|---|---|
| 기준선(base4) | **25:11 · 69%** | 15:3 | 10:8 | 10:2 | 8:4 | 7:5 |
| 실험 원판(nameF, `--extra`) | **27:9 · 75%** | 13:5 | 14:4 | 8:4 | 11:1 | 8:4 |

기준선 대비 69% 는 실험의 67% 보다 **높다.** 문구가 프롬프트 본문에 있으니
사용자의 `--extra` 자리를 비워 둔 채로도 효과가 난다.

원판보다 낫게 나온 건 뜻밖이지만 방향은 분명하다 — 특히 robber 11:1. 문체
블록에 심었을 때 원판에 0:12 로 지던 것이 자리를 옮기자 27:9 로 뒤집혔다.
**같은 문구, 다른 자리, 정반대 결과.**

이름 조항 최종 성적(누적 대결): 실험 24:12 · 재현 21:15 · 홀드아웃 25:11.

기계 검사로도 보인다 — 늦은 이름 base4 0.8 · nameC 0.7 · **nameF 0.2**.


### 5차 (8/27 낮) — 「한 대목만」. 유저분 지적에서 시작했다

> 「전체의 스토리를 포함하려고 하지 말고, 주요 장면만 따와서 이야기를 만들어보자.
> 너무 전체적으로 쓰려니까 컷도 어색하고 중구난방인 느낌이야.」

재 보니 그대로였다 — **도구는 영화의 80~90%를 훑고 있었다.** 12블록으로 그 폭을
덮으면 블록마다 7~8분씩 건너뛴다. 정답 3편은 긴 영화 둘에서 13%·21%만 썼다.

| 영화 | 현재 | 「한 대목만」 | 정답 |
|---|---|---|---|
| gentleman 111분 | 80~88% | **10 · 14 · 10%** | 21% |
| robber 97분 | 88~90% | **12 · 12 · 18%** | 13% |
| father 22분 | 82~85% | 82~85% (그대로) | 82% |

쌍대 36표 결과가 **길이로 정확히 갈렸다**:

| | 전체 훑기 : 한 대목만 |
|---|---|
| gentleman | 1 : **11** |
| robber | 6 : 6 |
| **긴 영화 둘 합계** | 7 : **17 (71%)** |
| father (22분 시트콤) | **11** : 1 |
| 전체 | 18 : 18 (50%) |

전체로는 50% 지만 **합쳐 보면 안 되는 숫자**다. father 는 구간이 똑같은데도
졌는데, 이유가 기계로 보인다 — 블록을 줄이고 대사를 밀어 넣었다
(12~13블록/53~58대사 → **11~12블록/60~64대사**). 「좁은 구간을 촘촘히」가
안 좁힌 영화에 걸리면 그냥 과밀이 된다.

처음엔 자막 길이로 잘랐다(40분). **유저분이 「그냥 모든 영화가 한 대목만 하도록
해」라고 해서 분기를 없앴다.** 대신 예외 문구를 빼서 짧은 영화도 실제로 한 대목을
잡게 했고, 「10~25분짜리 한 대목」이라고 **분량으로** 적는다(비율로 적으면 짧은
영화에서 3분을 채울 대사가 안 나온다). 프롬프트에 그 영화의 실제 길이도 적어 준다.

**유저분이 지정한 표본에서 완봉승 — 「캐치 미 이프 유 캔」(136분·영어 자막) 0:8**
(순서를 뒤집어도 4/4 동일). 긴 영화 누적 **7:25 (78%)**. **영화가 길수록 격차가
커진다** — 111분 1:11, 136분 0:8. 당연하다. 12블록으로 136분을 덮으면 블록마다
11분씩 건너뛴다.

심사평이 원인을 그대로 말한다:

> 「장면마다 한두 줄씩 스쳐 지나가 맥락 없는 대사가 많고」
> 「영화의 핵심인 모텔 사기를 6줄로 압축해 **왜 사기인지 전혀 전달되지 않는다**」
> 「하이라이트 나열에 가깝다」

반대쪽은 「배리 앨런 가명을 도입부에 심어 두고 마지막에 터뜨리는 하나의 설계된
줄기」다. **한 대목만 쓰면 셋업과 회수가 들어갈 공간이 생긴다.** 전체를 훑을
때는 그 공간이 없었다.

**외국어 분기가 실제 생성으로 처음 돌았다** — 4/4 성공, 번역 안 된 대사 줄 0개.
지금까지 이 경로는 selftest 픽스처로만 검증됐다. 다만 줄거리에 「프랭크
**아백내일** 주니어」가 나왔다(통용 표기는 아바그네일) — 이름 조항이 「널리
알려진 이름을 써라」인데 음차를 지어낸 셈이다. 대본 본문은 「프랭크」·「칼」만
쓰므로 화면에는 안 나간다. 표본이 쌓이면 다시 본다.

`AGENTS.md` 의 「구성은 따라 하지 마라 — 처음부터 결말까지 간다」를 철회했다.
그건 재 본 적 없는 판단이었고, 재 보니 반대였다.

**홀드아웃 통과 — 17:7 (71%).** 프롬프트에 박은 채로(길이 분기) 긴 영화 둘을
6판 새로 뽑아 24표. 쓴 구간도 실험 때와 같은 폭(14~20%)이고, 이름 조항 때처럼
자리 때문에 힘이 빠지는 일은 없었다.

| | 전체 훑기 : 한 대목만 |
|---|---|
| 실험 (gentleman·robber·catch) | 7 : 25 (78%) |
| 홀드아웃 (gentleman·robber) | 7 : 17 (71%) |
| **합계** | **14 : 42 (75%)** |

**robber 만 두 번 다 6:6 이다.** 나머지는 크게 이긴다(gentleman 11:1 두 번,
catch 8:0). 긴 영화에서 지는 경우는 없었고, 어떤 영화에서 덜 먹히는지는
표본이 더 쌓여야 안다.

### 되돌리면 안 되는 결정들

| 규칙 | 왜 |
|---|---|
| **대사 문체 검사는 번역 경로에서만 켠다** | 한국어 자막이면 대사가 자막 원문이라 사람이 못 고친다(규칙 6). 켜면 고칠 수 없는 ✘ 만 쌓인다. 재는 대상도 `script_io.authored_lines` 가 한 번 더 좁힌다 — `@시각`만 붙은 frozen 문단은 뺀다 |
| **한 덩어리의 여러 줄에 같은 시각을 주지 않는다** | `_stack` 이 길이를 「다음 자막까지」로 재므로 앞줄이 0초가 되어 버려진다. 실측으로 2줄 자막 10개의 첫 줄이 10개 전부 사라졌다 |
| **나레이션 조각은 한 문장이 한 단(row)** | 안 묶으면 8단 꼭대기에 4→13개가 몰리고, CapCut 에서 한 문장의 조각이 세 트랙에 흩어져 문장 단위로 스타일을 못 고친다 |
| 여유 `PRE/POST` 는 **이웃 자막 간격의 절반**을 넘지 않는다 | 자막 인접 간격의 28.7%가 0.083초(2프레임). 고정 0.5초면 연속 대사마다 소스가 0.417초 되감기고 같은 대사 끝소리가 두 번 들린다. 절반 클램프로 되감기 8건→0건 |
| 나레이션 길이 = **`timing.raw_weight × 0.12`** | 글자수 × 0.12가 아니다. 공백·문장부호는 가중치 0 — 409자는 317단위 = 38.0초 |
| 블록 헤더는 **읽을 때 권위값** | 파생값으로만 두면 b-roll 블록(250초)이 왕복마다 절반씩 수축한다 |
| 헤더는 **floor/ceil** | 정수초 표기가 실제 범위를 항상 덮어야 한다. floor/floor 40/48, round/round 46/48, **floor/ceil 48/48** |
| 동일 대사 동점 해소는 **필터가 아니라 선호** | 하드 필터면 사용자가 문단 순서를 바꾸는 순간 매칭이 죽는다 |
| 되감기 `raise` 는 **큐 번호가 앞으로 갈 때만** | 번호가 뒤로 가는 건 의도적 재배치다 |
| `subtitle.py` 는 `lines` 와 `src_no` 를 **둘 다** 보존 | 접으면 2줄 문단 13개가 뭉개지고, 재번호하면 SRT를 열었을 때 번호가 안 맞는다 |
| **제목은 자기 텍스트 트랙을 쓴다** | 나레이션 트랙에 얹으면 대본이 나레이션으로 시작할 때 둘 다 `t=0` 이라 CapCut 조립이 `세그먼트가 겹칩니다 (0 < 41667)` 로 죽는다. 화면에선 제목 y=+0.45 / 자막 y=−0.50 이라 애초에 안 겹친다 — `selftest [8]⑦` 이 고정 |
| 프로젝트마다 **새 timeline GUID** | 스켈레톤 GUID가 폴더명 1곳 + 4파일 5곳에 박혀 있다. `_reid_timeline()` 은 `resolve_timeline_id()` **앞에서** 돈다 |
| `script_gen` 은 기존 대본을 **지우지 않는다** | 생성 전에 지웠다가 에이전트 실패로 날아간 적이 있다 — `selftest [9]` 가 고정 |
| `script_gen` 은 **이미 있는 파일을 건드리지 않는다** | 같은 이름이 있으면 `_2`, `_3` 으로 옆에 만든다. 사람이 고친 대본을 덮어쓰면 되돌릴 수 없다 |
| 에이전트 프롬프트는 **파일로** 넘긴다 | `codex.cmd` 는 배치 래퍼라 여러 줄 인자를 `%*` 로 받으면서 줄바꿈이 뭉개진다 |
| 자식 프로세스 `stdin` 은 **닫는다**(`DEVNULL`) | 열어 두면 `codex exec` 이 프롬프트를 인자로 받고도 `Reading additional input from stdin...` 하고 EOF 를 기다린다 |
| `codex exec` 에 **`--skip-git-repo-check`** | 깃 저장소 밖이면 `Not inside a trusted directory` 로 거부한다 |
| `claude -p` 에 **`--allowedTools "Bash(python:*)"`** | `acceptEdits` 만으론 검증 명령(`cli.py check`)이 거부되고, 그러면 에이전트가 포기하는 대신 `cli.py`·`script_io.py`·`layout.py` 를 통째로 읽어 손으로 검산하느라 2분 30초짜리 작업이 8분이 된다 |
| 자식 PATH 에 **nodejs** 를 넣는다 | `codex.cmd` 가 `node` 를 못 찾으면 `'"node"' is not recognized` 로 죽는다 |
| 윈도우에서는 **`codex.cmd`** | 확장자 없는 `codex` 는 sh 스크립트라 CreateProcess 가 못 돌린다 |
| 에이전트 프롬프트는 **고쳐 쓰기 3회 상한** | 상한이 없으면 검산 루프에서 시간을 무한정 쓴다. 어차피 `pipeline.run_script` 가 뒤에서 다시 검사한다 |

### 확인 끝난 것 (CapCut 8.9.1 실물)

- 세로 캔버스는 **fit(레터박스)** — 1920×1080이 1080×1920 안에 온전히 들어가고 좌우가 안 잘린다.
- 자막이 한 번에 하나씩 뜨고 **자리는 맨 아래 고정**이다
  (대사 흰색 / 나레이션 연노랑). **단마다 트랙이 하나씩** + 제목 트랙 —
  단은 트랙을 고르는 데만 쓰고 y 는 안 움직인다(`layout.SUB_Y0`).
  `capcut_draft.SUB_ROW_STEP` 은 **없는 상수다** — 자막이 화면에서 기어
  올라가는 걸 멈추면서 지웠다.
- `codex` / `claude` 양쪽 CLI 인자 조합.

### 아직 안 끝난 것

1. 실제 영화 + 실제 TTS로 끝까지 한 편 만들어 보기.
2. 리캡 영상용 `.srt` 내보내기 없음 (`srt_export.py` 로 금방 붙는다).
